Extraction Settings:
  root search directory: "."
  sections:
    - name: "raw reads" 
      enabled: True
      multiQC:
        from json:
          - "raw_multiqc_report"
          - "raw_multiqc_data"
          - "multiqc_data.json"
        search recursively: True
        logs directory: 
          - "00-RawData"
          - "FastQC_Reports"
        logs pattern(s):
          - "*fastqc.zip"
        modules:
          - "fastqc"
